# ──────────────────────────────────────────────────────────────────────────────
# Core ASR / alignment
# ──────────────────────────────────────────────────────────────────────────────
faster-whisper>=1.0.0

# WhisperX for fine-grained word-level alignment (optional – pipeline
# degrades gracefully if unavailable).
# Install separately if you need it:
#   pip install git+https://github.com/m-bain/whisperX.git
# or uncomment the line below to include it in the Docker image:
# whisperx

# ──────────────────────────────────────────────────────────────────────────────
# Audio / ML runtime
# ──────────────────────────────────────────────────────────────────────────────
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
